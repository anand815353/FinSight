Install package     → uv add package
Install dev tool    → uv add --dev package
Remove package      → uv remove package
Recreate env        → uv sync
Run command         → uv run command
Inspect packages    → uv pip list / uv tree
pin uv python       → uv python pin 3.12.13
sync uv             → uv sync --dev
